# MFHelper Backend Dependencies

# Web Framework
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6  # File uploads

# Data Processing
pandas>=2.0.0
numpy>=1.24.0
openpyxl>=3.1.0  # Excel support

# PDF Processing
PyMuPDF>=1.23.0  # Better PDF parsing (fitz)
# casparser[fast] is optional — needs libpoppler-dev on Linux
# Install separately if CAS parsing via casparser is needed:
#   apt-get install -y libpoppler-dev pkg-config && pip install casparser[fast]>=0.7.0

# Database
sqlalchemy>=2.0.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0  # Environment variables
alembic>=1.13.0  # Database migrations

# PostgreSQL drivers (required for production on Render)
psycopg2-binary==2.9.9  # PostgreSQL adapter
# asyncpg==0.29.0  # Optional: Async PostgreSQL driver

# Authentication
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt<4.1.0  # passlib 1.7.4 incompatible with bcrypt >= 4.1
python-multipart==0.0.6
authlib>=1.3.0  # OAuth 2.0 client (Google, GitHub, etc.)

# HTTP Client (for RTA APIs)
httpx>=0.26.0
aiohttp>=3.9.1

# Financial Calculations
# scipy and pyxirr are optional heavy deps — uncomment if needed
# scipy>=1.12.0
# pyxirr>=0.10.6  # XIRR calculation

# Validation
pydantic>=2.5.3
pydantic-settings>=2.1.0
email-validator==2.1.0

# Utilities
python-dotenv==1.0.0
python-dateutil==2.8.2

# Caching
redis==5.0.1

# Rate Limiting
slowapi==0.1.9

# Data Scraping & Ingestion
beautifulsoup4>=4.12.0
lxml>=5.0.0
requests>=2.31.0

# Scheduling (optional — not used in current startup)
# APScheduler>=3.10.0

# Testing
pytest==7.4.4
pytest-asyncio==0.23.3
pytest-cov==4.1.0  # Coverage reporting
pytest-html==4.1.1  # HTML test reports
pytest-xdist==3.5.0  # Parallel test execution
httpx==0.26.0

# Logging
# loguru==0.7.2  # Optional — using stdlib logging instead

# Monitoring & Error Tracking
sentry-sdk>=1.40.0  # Error tracking and performance monitoring
# prometheus-client>=0.19.0  # Optional metrics collection
psutil>=5.9.0  # System and process utilities (optional, gracefully handled)

# AI/ML - Ollama Integration (Local TinyLlama)
requests>=2.31.0  # HTTP client for Ollama API

# Blog & Content Management
python-frontmatter>=1.0.0  # Parse markdown with YAML front matter
markdown>=3.5.0  # Markdown to HTML conversion
Pygments>=2.17.0  # Syntax highlighting for code blocks
